'use strict';

const {
  Utils
} = require(`./utils`);

const {
  generateCategory,
  generateTitle,
  generateAnnounce,
  generateFullText,
  generateDate,
  generateComments
} = require(`./generatePublications`);

module.exports = {
  Utils,
  generateCategory,
  generateTitle,
  generateAnnounce,
  generateFullText,
  generateDate,
  generateComments
};
