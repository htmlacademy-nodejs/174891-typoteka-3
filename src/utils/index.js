'use strict';

const {
  Utils,
  ensureArray
} = require(`./utils`);

const {
  generateCategory,
  generateTitle,
  generateAnnounce,
  generateFullText,
  generateDate,
} = require(`./generatePublications`);

module.exports = {
  Utils,
  ensureArray,
  generateCategory,
  generateTitle,
  generateAnnounce,
  generateFullText,
  generateDate,
};
