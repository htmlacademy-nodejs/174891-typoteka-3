'use strict';

const {
  Utils
} = require(`./utils`);

const {
  generateCategory,
  generateTitle,
  generateAnnounce,
  generateFullText,
  generateDate
} = require(`./generatePublications`);

module.exports = {
  Utils,
  generateCategory,
  generateTitle,
  generateAnnounce,
  generateFullText,
  generateDate
};
